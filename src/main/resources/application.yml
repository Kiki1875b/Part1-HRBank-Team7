spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hrbank
    username: postgres
    password: 970501
  jpa:
    hibernate:
      ddl-auto: none #'update' 로 처음 실행 후 'none' 으로 변경
    show-sql: true
    open-in-view: false
  batch:
    job:
      enabled: false
    jdbc:
      initialize-schema: always


hrbank:
  storage:
    local.root-path: "C:\\hrbank-storage" # users.dir 고려
    backup: "$C:\\backups"
    file-name: "/tmpBackup.csv"
