spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hrbank
    username: hrbank
    password: 1234
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    open-in-view: false
  batch:
    jdbc:
      initialize-schema: always


hrbank:
  storage:
    local.root-path: "C:\\hrbank-storage"
    backup: "${user.dir}/backups"
